{
  "name": "fraction-vis",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:pages": "vite build --base=/fraction_vis/",
    "deploy": "npm run build:pages && gh-pages -d dist -b gh-pages",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "katex": "^0.16.45"
  }
}
